[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--no-audit --no-fund"

# Security headers and SPA redirects live in public/_headers and
# public/_redirects respectively — copied into dist/ at build time and
# applied by Netlify's edge.
